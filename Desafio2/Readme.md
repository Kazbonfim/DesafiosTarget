# Projeto para Gerenciar Produtos em um Estoque
## Simples, para executar, e testar algumas funcionalidades 

Para executar, rode a partir das raízes: backend & frontend

```
npm install # backend, e frontend, isso vai instalar as dependências do projeto
npm run dev # backend
npm run dev # frontend
```